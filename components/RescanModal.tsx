"use client"

// Archived stub for RescanModal.
// Kept for compatibility with generated source maps / references.
// TODO: fully remove when build artifacts no longer reference this path.

import React from "react"

interface RescanModalProps {
  scanId?: string
  fileName?: string
  onClose?: () => void
  onRescanComplete?: (result: any) => void
}

export function RescanModal(_props: RescanModalProps) {
  // Archived: no-op stub to avoid missing-file errors.
  return null
}

export default RescanModal
